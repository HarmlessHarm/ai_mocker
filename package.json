{
  "name": "ai-mocker-monorepo",
  "version": "0.0.1",
  "description": "AI-powered mock data generator for MSW.js based on Zod schemas",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "test": "pnpm -r test",
    "generate:mocks": "pnpm --filter msw-service generate"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "tsup": "^8.3.0",
    "vitest": "^2.1.0"
  }
}
